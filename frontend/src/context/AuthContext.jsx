/**
 * AuthContext.jsx — Contexte d'authentification
 *
 * TODO:
 * - login(email, password) → appeler API, stocker JWT
 * - logout() → supprimer JWT
 * - user, isAuthenticated state
 * - Stocker le token dans localStorage
 */

// TODO: createContext, AuthProvider, useAuth hook
